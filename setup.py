from setuptools import setup, find_packages

setup(
    name="numToEng",
    version="0.0.1",
    description="A toy package for converting numbers to written out English.",
    author_email="contact@elijah.solutions",
    packages=find_packages()
)